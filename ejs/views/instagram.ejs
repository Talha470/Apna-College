<%- include("includes/head") %>
  <% for(let post of data.posts){%> 
    <img src="<%= post.image %>" alt="Some image" srcset="">
    <p>Likes :<%= post.likes %> &nbsp; &nbsp;&nbsp;&nbsp;
      Comments :<%= post.comments %>
      </p>
    <% } %>
</body>
</html>